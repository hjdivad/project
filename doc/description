
Goals

	Setup gitosis-admin
	Create repo with gvimrc, initial Rakefile, etc.


project <name> [<template>]

	{1} creates directory <name>, copied from <template>, if given.
	{2} initializes as git repository with root commit.
	{3} runs post-creation script which might, for example, add to projects









 vim:ft=help:
